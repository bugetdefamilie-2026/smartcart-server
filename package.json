{
  "name": "smartcart-lidl-proxy",
  "version": "1.0.0",
  "description": "Proxy server that downloads, parses and caches the daily LIDL .xlsb price file",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "xlsx": "^0.18.5"
  }
}
